#include <stdio.h>
/**
 * main - Entry point
 *
 * Description: Print a specific sentance using printf() function
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,");
return (0);
}
